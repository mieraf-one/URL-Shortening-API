# URL-Shortening-API

# FastAPI URL Shortener with JWT Authentication

A backend API built with FastAPI that provides user authentication 
(access & refresh tokens) and a URL shortening service with expiration support.
